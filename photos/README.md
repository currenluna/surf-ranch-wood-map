Place camera photos here, named `po-01.jpg`, `po-02.jpg`, ... `po-32.jpg`
(camera id zero-padded to 2 digits).

The viewer looks up `photos/po-<NN>.jpg` for whichever camera is selected and
shows a placeholder message if the file is missing.
